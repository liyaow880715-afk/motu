const { OpenAICompatibleAdapter } = require('../.next/server/chunks/lib_ai_adapters_openai-compatible_ts.js');

// Direct test - but this won't work easily with Next.js compiled chunks
console.log('This approach wont work well with Next.js compiled output.');
